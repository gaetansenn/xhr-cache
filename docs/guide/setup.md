# Setup

Install with yarn:

```bash
yarn add @dewib/xhr-cache
```

Install with npm:

```bash
npm install @dewib/xhr-cache
```

Edit `nuxt.config.js`:

```js
modules: [
  '@dewib/xhr-cache'
]

xhrCache: {
  // Options
}
```